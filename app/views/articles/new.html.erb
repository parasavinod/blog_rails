<h1>New Article</h1>

<%= render "form", article: @article %>
<%# A partial's filename must be prefixed with an underscore, %>
 <%# e.g. _form.html.erb. But when rendering, it is referenced without the underscore, e.g. render "form". %>
